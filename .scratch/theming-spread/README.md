# theming-spread — two deferred visual-consistency threads

Status: backlog

Two open threads, both raised and deliberately deferred during earlier efforts rather than
scoped and built. Neither is chartered yet — this directory exists so they aren't lost, not
because either is ready to build. See `issues/` for each.

`issues/` — `grep '^Status:' issues/*.md` is the authoritative view. Both start at
`needs-triage`: they need an owner conversation to pick scope and, for 01, a variant round,
before either is `ready-for-agent`.
